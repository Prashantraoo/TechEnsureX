# TechEnsureX — Frontend

React + Vite + TypeScript frontend. See the [repository README](../README.md) for
setup, environment variables, and how this app fits together with the backend.

```bash
npm install
npm run dev
```
