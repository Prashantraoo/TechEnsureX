# Local dev: point at the backend's dev server directly.
VITE_API_URL=http://localhost:5000/api

# Production (Vercel Services, one domain for both frontend and backend):
# use a same-origin relative path instead of an absolute URL.
#   VITE_API_URL=/api
